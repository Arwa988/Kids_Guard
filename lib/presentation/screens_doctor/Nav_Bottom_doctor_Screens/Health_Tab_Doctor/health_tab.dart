import 'package:flutter/material.dart';

class HealthTabDoctor extends StatelessWidget {
  const HealthTabDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}